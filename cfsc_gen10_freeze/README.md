# CFSC-SVL Gen10 public pre-target freeze

This directory is the public pre-target commitment for `CFSC_SOUND_STATEMENT_BOUND_VALIDATION_LIFECYCLE_GEN10`.

## Corrected authoritative freeze

Frozen before any **correct target-artifact** workflow execution:

- 60 source-census records;
- 24 qualifying workflows: 16 primary and 8 reserve;
- 15 external projects, 6 scientific workflow categories and 17 certificate classes;
- target-sized canonical input artifact bytes and semantic/physical digests;
- exact expected scientific statements, policies and revocation locators;
- B0–B10 strongest-baseline definitions;
- lifecycle model, timing protocol, attack classes and S0–S9 gates;
- authoritative complete freeze SHA-256 `b901e64e6d64fc231cd7775e304fa3de282ccbe397e2bae8246ef46daf9cbc09`.

## Preserved development freeze failure

The earlier freeze SHA `f6b1805d59f9aa5b4cad0456eaf7dae52b425eb4beae3ef65d875d5f0cb2917e` incorrectly canonicalized smoke-sized inputs while naming target sizes. The sound verifier rejected the first attempted workflow with `PARAMETERS`; no corrected target result existed. That freeze is superseded, retained as development evidence and receives no scientific credit.

The Gen9 predictor verdict remains `CFSC_FRESH_CORPUS_REPLICATION_FAILED` and is not rescored. This is a preregistration commitment, not external-team replication, lifecycle establishment or Nature readiness.
